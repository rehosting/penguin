core:
  root_shell: false

env:
  igloo_init: /init


static_files:
  /init:
    type: inline_file
    contents: |
      #!/igloo/utils/sh
      set -ex

      # start server
      /igloo/utils/busybox httpd -f -p 8000 &
      sleep 1
      echo "tests pass"
      exit 0
    mode: 73
