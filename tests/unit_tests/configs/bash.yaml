core:
  root_shell: false

env:
  igloo_init: /init

static_files:
  /init:
    type: inline_file
    contents: |
      #!/igloo/utils/bash

      echo "Hello from $0 $@"
      for x in a b c d; do
        echo $x
      done
    mode: 73
